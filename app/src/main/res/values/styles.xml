<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="MyMaterialButton" parent="AppTheme">
        <item name="boxCornerRadiusTopStart">@dimen/btn_corner_radius</item>
        <item name="boxCornerRadiusTopEnd">@dimen/btn_corner_radius</item>
        <item name="boxCornerRadiusBottomStart">@dimen/btn_corner_radius</item>
        <item name="boxCornerRadiusBottomEnd">@dimen/btn_corner_radius</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">0dp</item>
    </style>

</resources>
